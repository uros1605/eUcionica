@page
@model eUcionica.Pages.Predmeti.PredmetiModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Predmeti</h1>

<p>
    <a asp-page="Create">Dodajte novi predmet</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Predmet[0].Naziv)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Predmet) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Naziv)
            </td>
            <td>
                    <a asp-page="./Edit" asp-route-id="@item.PredmetID">Uredi</a> |
                    <a asp-page="./Details" asp-route-id="@item.PredmetID">Detalji</a> |
                    <a asp-page="./Delete" asp-route-id="@item.PredmetID">Obriši</a>
            </td>
        </tr>
}
    </tbody>
</table>
